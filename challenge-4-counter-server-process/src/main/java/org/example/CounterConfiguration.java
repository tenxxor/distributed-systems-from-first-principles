package org.example;

import io.dropwizard.core.Configuration;

/**
 * Empty for now — we have no custom config fields yet. Everything the server
 * needs (ports, logging) is covered by Dropwizard's built-in Configuration.
 */
public class CounterConfiguration extends Configuration {
}
